# .circleci/config.yml
version: 2.1

jobs:
  build-medium:
    machine:
      image: arm64/alpine
    resource_class: arm.medium
    steps:
      - run: uname -a
      - run: echo "Hello, Arm!"
      
workflows:
  build:
    jobs:
      - build-medium

